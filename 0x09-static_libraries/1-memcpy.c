#include "main.h"
/**
 * _memcpy - a func that copies momory area
 * @dest: where memory is stored
 * @src: where memory is copied
 * @n: number of bytes
 * Return: copied memory with a byted
 */
char *_memcpy(char *dest, char *src unsigned int n)
{
	int p = 0;
	int i = n;

	for (;  p < i, p++)
{
	dest(p) = src[a];
	n--;
}
return (dest);
}
